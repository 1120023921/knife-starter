plugins {
    id 'org.springframework.boot' version '3.0.5'
    id 'io.spring.dependency-management' version '1.1.0'
    id 'java-library'
}

allprojects {
    apply plugin: 'java-library'
    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    sourceCompatibility = '17'

    ext {
        set('springBootVersion', "3.0.5")
        set('springCloudVersion', "2022.0.2")
        set('mybatisPlusBootVersion', "3.5.3.1")
        set('mybatisPlusGeneratorVersion', "3.5.3.1")
        set('hutoolVersion', "5.8.18")
        set('knife4jVersion', "3.0.3")
        set('jacksonDatabindVersion', "2.15.0")
        set('dom4jVersion', "2.1.4")
        set('slf4jApiVersion', "2.0.7")
        set('dysmsapiVersion', "2.0.23")
    }

    dependencies {
        implementation 'org.springframework.boot:spring-boot-autoconfigure'
        annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'
        annotationProcessor 'org.projectlombok:lombok'
        compileOnly 'org.projectlombok:lombok'
    }

    dependencyManagement {
        imports {
            mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudVersion}"
        }
    }

    processResources {
        from('src/main/java') {
            include '**/*'
        }
    }

    configurations {
        compileOnly {
            extendsFrom annotationProcessor
        }
    }

    jar {
        enabled = true
        archiveClassifier = ''
    }

//    task javadocJar(type: Jar) {
//        from javadoc
//        classifier = 'javadoc'
//    }
}
